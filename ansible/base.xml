---
- hosts: localhost

  tasks:
    - name: Update all pacakges
      dnf:
        name: "*"
        state: latest

    - name: Install some packages
      dnf:
        name: chromium,mate-terminal,neofetch,htop,ncdu,git,et,parted,zip,unzip,file,wget,traceroute,android-tools,mosh,fwupd,rclone,whois,bc,nmap,meld,openvpn,glxinfo,gparted,lsof,gimp,shotwell,mate-utils,eom,audacity,sox,vlc,yt-dlp,id3v2,lame,id3lib,mp3gain,flatpak-builder,gnome-tweaks,protonvpn-cli,qbittorrent,gnome-flashback

# need rpmfusion for these
# ffmpeg,ffmpeg-normalize,cdrkit

    - name: Install dnf plugins
      dnf:
        name: dnf-plugins-core

    - name: Install brave repo
      shell:
        dnf config-manager --add-repo https://brave-browser-rpm-release.s3.brave.com/brave-browser.repo && rpm --import https://brave-browser-rpm-release.s3.brave.com/brave-core.asc

    - name: Install Brave
      dnf:
        name: brave-browser




    - name: Start et service if not started
      service:
        name: et
        state: started
        enabled: yes

    - name: add prompt script
      copy:
        src: prompt.sh
        dest: /etc/profile.d/prompt.sh
        owner: root
        group: root
        mode: 0644

